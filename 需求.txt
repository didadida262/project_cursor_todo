1. Create a new directory.

2. I plan to develop a simple todo application using vanilla HTML, CSS, JavaScript. Please suggest a clear file structure.

3. Manually create 3 files: index.html, style.css, script.js.

4. @index.html Create an HTML page structure for a todo application. It should include a title, a form for entering new tasks (including an input field and an add button), and an ordered list for displaying todo items. Please use semantic tags and link to style.css and script.js files.

5. Please design a modern, clean CSS style for @index.html. The main content should be centered with a maximum width of 800px. The input field and button styles should be attractive. There should be spacing between list items. When hovering over buttons and list items, there should be visual feedback effects.

6. @index.html @style.css Implement the add todo functionality in JavaScript. When the user clicks the add button, get the content from the input field, create a new li element and add it to the list, then clear the input field after adding.

7. In JavaScript, implement the mark complete and delete functionality. Each list item (li) should contain a 'Complete' button and a 'Delete' button. Clicking the 'Complete' button should add a 'completed' CSS class to that list item (for strikethrough text). Clicking the 'Delete' button should remove that list item from the list.

8. In JavaScript, use LocalStorage to store todo data, read and display it when the page loads; when adding, completing, or deleting, synchronously update LocalStorage.

9. In JavaScript, implement filtering with conditions "All", "Active", "Completed", and bottom functions "Clear Completed", "Clear All".
